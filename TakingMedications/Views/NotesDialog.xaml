<Window x:Class="TakingMedications.Views.NotesDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Заметки"
        Width="480" Height="560"
        MinWidth="360" MinHeight="360"
        ResizeMode="CanResizeWithGrip"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource BgDarkBrush}">

    <Window.Resources>
        <!-- Colored circle RadioButton for note color picker -->
        <Style x:Key="ColorDotRadio" TargetType="RadioButton">
            <Setter Property="GroupName" Value="NoteColor"/>
            <Setter Property="Margin" Value="0,0,5,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="Bd"
                                Width="20" Height="20" CornerRadius="10"
                                Background="{TemplateBinding Background}"
                                BorderThickness="2.5" BorderBrush="Transparent"
                                Cursor="Hand"/>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.85"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock x:Name="HeaderLabel" Grid.Row="0"
                   FontSize="18" FontWeight="Bold"
                   Foreground="{DynamicResource TextPrimaryBrush}"
                   Margin="0,0,0,12"/>

        <!-- Notes list -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,0,0,10">
            <StackPanel x:Name="NotesList"/>
        </ScrollViewer>

        <!-- Add area -->
        <Border Grid.Row="2"
                Background="{DynamicResource BgCardBrush}"
                BorderBrush="{DynamicResource CardBorderBrush}"
                BorderThickness="1" CornerRadius="8"
                Padding="10,8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Text input -->
                <TextBox x:Name="NewNoteBox" Grid.Row="0"
                         AcceptsReturn="True" TextWrapping="Wrap"
                         MinHeight="54" MaxHeight="120"
                         VerticalScrollBarVisibility="Auto"
                         Background="Transparent"
                         Foreground="{DynamicResource TextPrimaryBrush}"
                         BorderThickness="0"
                         FontSize="13"
                         Padding="2,2,2,4"
                         Margin="0,0,0,6"
                         KeyDown="NewNoteBox_KeyDown"
                         GotFocus="NewNoteBox_GotFocus"
                         LostFocus="NewNoteBox_LostFocus"/>

                <!-- Color picker + Add button -->
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock x:Name="LblColor"
                                   Foreground="{DynamicResource TextSecondaryBrush}"
                                   FontSize="12" VerticalAlignment="Center"
                                   Margin="0,0,6,0"/>
                        <RadioButton x:Name="RbColorNone"   Style="{StaticResource ColorDotRadio}" Background="#555555" Tag=""       IsChecked="True"/>
                        <RadioButton x:Name="RbColorYellow" Style="{StaticResource ColorDotRadio}" Background="#F9A825" Tag="yellow"/>
                        <RadioButton x:Name="RbColorGreen"  Style="{StaticResource ColorDotRadio}" Background="#388E3C" Tag="green"/>
                        <RadioButton x:Name="RbColorRed"    Style="{StaticResource ColorDotRadio}" Background="#C62828" Tag="red"/>
                        <RadioButton x:Name="RbColorBlue"   Style="{StaticResource ColorDotRadio}" Background="#1565C0" Tag="blue"/>
                    </StackPanel>

                    <Button x:Name="BtnAdd" Grid.Column="2"
                            Click="BtnAdd_Click"
                            Padding="14,6"
                            FontSize="13"
                            Background="{DynamicResource AccentBrush}"
                            Foreground="White"
                            BorderThickness="0">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}"
                                        CornerRadius="6"
                                        Padding="{TemplateBinding Padding}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>
