<UserControl x:Class="TakingMedications.Views.ForecastView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource BgDarkBrush}">

    <Grid Margin="0,8,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ── Прогноз запаса ── -->
        <Border Grid.Row="0" Background="{DynamicResource BgCardBrush}"
                CornerRadius="12" Padding="14,10" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal">
                <TextBlock x:Name="LblForecastTitle"
                           FontSize="16" FontWeight="Bold"
                           Foreground="{DynamicResource TextPrimaryBrush}"
                           VerticalAlignment="Center" Margin="0,0,16,0"/>
                <TextBlock x:Name="LblForecastSubtitle"
                           FontSize="12"
                           Foreground="{DynamicResource TextSecondaryBrush}"
                           VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Background="{DynamicResource BgCardBrush}"
                CornerRadius="12" Padding="10,8" Margin="0,0,0,10">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="ForecastPanel"/>
            </ScrollViewer>
        </Border>

        <!-- ── Список покупок ── -->
        <Border Grid.Row="2" Background="{DynamicResource BgCardBrush}"
                CornerRadius="12" Padding="14,10" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal">
                <TextBlock x:Name="LblShoppingTitle"
                           FontSize="16" FontWeight="Bold"
                           Foreground="{DynamicResource TextPrimaryBrush}"
                           VerticalAlignment="Center" Margin="0,0,16,0"/>
                <TextBlock x:Name="LblShoppingSubtitle"
                           FontSize="12"
                           Foreground="{DynamicResource TextSecondaryBrush}"
                           VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="3" Background="{DynamicResource BgCardBrush}"
                CornerRadius="12" Padding="10,8">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="ShoppingPanel"/>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
